export interface Props {
    category: {id: number, label: string};
    id: number;
    price: number;
    serving: number;
    size: number;
    photo: string;
    description: string;
    title: string;
}